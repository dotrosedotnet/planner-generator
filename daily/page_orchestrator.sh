#!/bin/env bash

# this document will take the settings from each sections' `settings.md`, as
# well as the page's `settings.md` and apply them to the page parts it will
# also lay the page parts out on the page

echo 1

